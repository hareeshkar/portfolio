// File: /src/particles-config.js

// This file is now merged into particle-presets.js for hero. If needed separately, it can be used as a fallback.
// For consistency, we've enhanced and integrated the hero config directly into presets.
export default () => console.warn('Use getParticlePresets for all configs');